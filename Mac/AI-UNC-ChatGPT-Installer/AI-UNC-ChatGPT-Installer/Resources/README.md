# AI @ UNC ChatGPT Installer Resources

This folder is reserved for user-facing support material and future packaged assets.

The app currently embeds the recommended Codex configuration in Swift code so a future release can replace it with a UNC-hosted JSON source without changing the dashboard or update flow.
